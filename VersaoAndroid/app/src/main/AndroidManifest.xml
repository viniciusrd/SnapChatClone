<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.vinicius.android.snapchatclone">

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".ViewController.acitivity.SnapsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ViewController.acitivity.LoginActivity" />
        <activity
            android:name=".ViewController.acitivity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewController.acitivity.CreateAccountActivity" />
        <activity android:name=".ViewController.acitivity.PhotoActivity" />
        <activity android:name=".ViewController.acitivity.UsersActivity" />
        <activity android:name=".ViewController.acitivity.DetailSnapActivity"></activity>
    </application>

</manifest>